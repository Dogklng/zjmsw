var user_agent = navigator.userAgent;

if(user_agent.search(/iPhone|iPad/) == -1) {
	
	var scrollbar;
	$(function () {

			scrollbar = new ScrollBar('#whatsnew .list ul', '#whatsnew .list');
		}
	);

}

